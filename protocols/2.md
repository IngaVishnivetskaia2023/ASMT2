# GraphQL

GraphQL — это инструмент, который соединяет клиент и сервер для удобного получения данных. Он:

- Не зависит от базы данных или способа передачи данных.
- Позволяет делать сложные запросы к серверу с вложенными данными.
- Работает с известными типами данных, что предотвращает ошибки в разработке и улучшает интеграцию с современными инструментами вроде TypeScript.

## Особенности:

- **Database-agnostic — "Независим от базы данных"**
Это значит, что GraphQL не привязан к конкретной базе данных.
Он работает как "посредник" между клиентом (например, вашим приложением) и любым источником данных.
Это может быть реляционная база данных вроде MySQL или PostgreSQL, NoSQL, файлы, другие API и т.д.
Сервер GraphQL может получать данные из разных систем и объединять их для клиента — клиенту всё равно, откуда данные берутся.

🚀 Пример: Ваш сервер может забрать данные о пользователях из MySQL, а историю их покупок — из MongoDB.
Клиенту об этом знать не нужно — он просто отправляет запрос, а GraphQL сам разберётся.

- **Transport-agnostic — "Независим от способа передачи данных"**
GraphQL не требует конкретного протокола для передачи данных.
Конечно, чаще всего используется `HTTP`, но вы можете использовать всё что угодно (например, `WebSocket` для реального времени).
Это гибкость, которая позволяет использовать GraphQL практически везде, где есть возможность передать запрос и получить ответ.

🚀 Пример:
- По HTTP вы отправляете запрос к серверу и получаете ответ.
- По WebSocket можно отправлять запросы в двух направлениях (например, для чатов или других потоковых данных).



- **Клиент-серверная архитектура, похожая на RPC — "Запросы, похожие на удалённые функции"**

GraphQL позволяет вашему клиенту (например, браузеру, приложению) запросить определённые данные у сервера.
Это похоже на вызов функции: вы отправляете запрос с входными данными, сервер выполняет "функцию" на своей стороне, а затем возвращает результат (выходные данные).

🚀 Пример: Если у вас есть сервер с функцией `getUserInfo(userId)`, клиент отправляет запрос с `userId` = 5, а сервер возвращает данные пользователя с `ID` = 5.



- **главное отличие от `RPC` — "Сложные вложенные запросы в одном запросе"**

В GraphQL вы можете запросить несколько связанных данных за один раз.
Вместо того чтобы делать несколько отдельных запросов, клиент отправляет один запрос и получает всё нужное, включая объекты и вложенные данные.
Это экономит время и уменьшает количество запросов к серверу.

🚀 Пример: В REST вам нужно делать несколько запросов:
- Получить информацию о пользователе.
- Получить его список покупок.
- Получить детали каждой покупки.
В GraphQL вы отправляете один запрос, который возвращает всё сразу, например:
```
query {
  user(id: 5) {
    name
    email
    purchases {
      id
      product {
        name
        price
      }
    }
  }
}
```



- GraphQL строго типизирован — "Типы данных заранее известны"
В GraphQL каждое поле и его параметры имеют фиксированный тип данных (например, Int, String, Boolean и т.д.).



# Сравнение GraphQL и REST

| **Проблема**          | **GraphQL**                                                                                              | **REST**                                                                                      |
|------------------------|---------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------|
| **Авторизация**        | GraphQL не имеет встроенной поддержки авторизации. Нужно реализовывать её самостоятельно. Например, можно использовать JWT (токены), но реализации сильно зависят от проекта. Без строгой проверки можно случайно "утечь" данные. | REST часто опирается на аутентификацию и авторизацию, реализованную на уровне маршрутов (endpoint'ов). Например, можно ограничить доступ к `/admin` API. Обычно проще и интуитивнее настроить. |
| **N+1 проблема**       | Из-за вложенных запросов в GraphQL сервер может генерировать слишком много обращений к базе данных (например, один запрос для пользователей и множество — для их постов). Это может привести к падению производительности. Решается через `DataLoader` для группировки запросов и кэширования результатов. | В REST данная проблема встречается реже, так как endpoints заранее оптимизированы для получения необходимых данных. Например, запрос `/users/with-posts` заранее продуман для возвращения полной информации. |
| **Сложность API**      | Легко создать слишком сложную или запутанную GraphQL схему. Плохой дизайн схемы может привести к неразберихе как для разработчиков, так и для клиентов, а также снизить производительность сервера. Требуется много усилий, чтобы построить "хороший" API. | В REST всё проще — каждый endpoint четко определён и получает доступ к определённым ресурсам. Например, `/users` только для пользователей, `/posts` только для постов. Даже плохой REST API обычно понятнее, чем плохой GraphQL API. |
| **Размер бандла**      | Запросы GraphQL являются частью клиентского кода (в отличие от REST, где запросы — это просто URL). Если запросы сложные, они увеличивают общий размер клиентского бандла (например, JavaScript-кода в браузере). Это может влиять на время загрузки приложения. | В REST запросы — это просто строки (например, `GET /users`). Они не добавляют веса к клиентскому коду и практически не влияют на размер бандла. |
| **Версионирование**    | У GraphQL нет встроенного механизма версионирования. Все изменения API должны быть тщательно спланированы. Например, вместо удаления старых полей их помечают как устаревшие (`@deprecated`). Разные версии API можно поддерживать через отдельные схемы, но это сложнее. | REST API работает с версиями очень просто. Вы просто создаёте новую версию endpoint'а, например, `/v1/users` для первой версии и `/v2/users` для новой версии. Старые клиенты продолжают использовать старую версию без изменений. |
Схема (schema) на сервере описывает, какие структуры данных можно запросить и какие параметры передать.
Эта строгость позволяет использовать статическую проверку (ошибки выявляются сразу на этапе написания кода, до выполнения).
GraphQL прекрасно работает с инструментами вроде TypeScript и статическими анализаторами кода.

🚀 Пример: Если сервер ожидает, что поле `age` будет `Int` (целым числом), а вы передали строку "25", GraphQL сразу же скажет, что это ошибка.

