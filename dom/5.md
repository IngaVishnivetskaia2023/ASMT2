## Performance tab

RAIL — это модель, придуманная Google для оценки и улучшения производительности веб-приложений с точки зрения пользователя.
Название — это аббревиатура из 4 этапов взаимодействия пользователя с сайтом:

#### R (Response) — Отклик
Сайт должен реагировать на действия пользователя (клик, скролл, ввод) за 100 мс.

#### A (Animation) — Анимация
Анимации и скролл должны быть плавными, обновляться 60 раз в секунду (каждый кадр за ~16 мс).

#### I (Idle) — Пауза
Когда пользователь ничего не делает, браузер может выполнять фоновую работу, но не мешать отклику.

#### L (Load) — Загрузка
Сайт должен быть готов к взаимодействию менее чем за 1 секунду на мобильных устройствах.

<img width="1390" height="504" alt="image" src="https://github.com/user-attachments/assets/2f135580-ff77-41ad-8c21-89f3def5a857" />


<img width="1918" height="572" alt="Screenshot (2189)" src="https://github.com/user-attachments/assets/506dfbbb-538e-48e2-bbb8-15b3bb782118" />

<img width="454" height="371" alt="Screenshot (2191)" src="https://github.com/user-attachments/assets/71dc54e1-4b41-4573-ad02-03de7cb7d652" />

#### DCL
The `DOMContentLoaded` event fires **when the HTML document has been completely parsed, and all deferred scripts** (`<script defer src="…">` and `<script  type="module">`) have downloaded and executed. It doesn't wait for other things like images, subframes, and async scripts to finish loading.

`DOMContentLoaded` does not wait for stylesheets to load, however deferred scripts do wait for stylesheets, and the DOMContentLoaded event is queued after deferred scripts. Also, scripts which aren't deferred or async (e.g., `<script>`) will wait for already-parsed stylesheets to load.

- `DOMContentLoaded` ensures your code runs when the DOM is ready, but before all images and resources are loaded.
- It’s used for safe, early, and efficient initialization of **scripts** that **work with the page structure**.

#### FCP

First Contentful Paint (FCP) is when the **browser renders the first content from the DOM**, providing feedback to the user that the page is loading. Completing the first contentful paint answers the question "Is it happening?"

The First Contentful Paint timestamp is when the browser first rendered any text, image (including background images), video, canvas that had been drawn into, or non-empty SVG. This excludes any content of iframes, but includes text with pending webfonts. This is the first time users could start consuming page content.

#### L
The load event is fired **when the whole page has loaded**, including all dependent resources such as stylesheets, scripts (including async, deferred, and module scripts), iframes, and images, **except those that are loaded lazily**. This is in contrast to `DOMContentLoaded`, which is fired as soon as the page DOM has been loaded, without waiting for resources to finish loading.

<img width="914" height="432" alt="Screenshot (2190)" src="https://github.com/user-attachments/assets/061eaf2f-9a3d-46d2-b201-be7d5b09fe8e" />

<img width="901" height="446" alt="Screenshot (2192)" src="https://github.com/user-attachments/assets/bed790b5-11fe-4437-ba02-c16ea0a223b3" />


#### "Recalculate Style" in "Bottom-Up" Actions

- The initial "Recalculate Style" includes building the CSSOM and applying styles.
- Later "Recalculate Style" events usually just re-apply existing CSSOM rules to changed elements.

#### Render Tree

<img width="1184" height="467" alt="Screenshot (2208)" src="https://github.com/user-attachments/assets/251cda62-20cd-4224-b1da-e0679bf0a35d" />

| Element Type | Included in Render Tree? | Reason |
| :-- | :-- | :-- |
| `<script>`, `<style>`, `<meta>`, `<head>`, `<title>`, `<link>` | No | Non-visual, not rendered |
| display: none` | No | Hidden by CSS |
| HTML comments | No | Not visual |
| `<template>` content | No (until used) | Not rendered until instantiated |



#### Layout
The layout stage is where the browser figures out exactly where and how big every visible element is, using the render tree and CSS rules, before painting pixels to the screen.

| Step | Description |
| :-- | :-- |
| Build Render Tree | Combine DOM + CSSOM, exclude non-visual elements |
|  |  |
| Determine Contexts | Block, inline, flex, grid, etc. |
| Calculate Boxes | Compute size, position, margins, borders, padding |
| Resolve Dependencies | Parent/child size dependencies, recursive calculation |
| Special Layouts | Flexbox, grid, floats, tables |
| Viewport/Scrolling | Handle overflow, scrollbars |
| Output Coordinates | Final positions/sizes for painting |

#### Technical Steps of the Layout Stage

🌴 **Input: Render Tree:**
The browser first builds the render tree by combining the DOM and CSSOM.
The render tree contains only visible elements (excluding those with display: none, etc.).

🌴 **Formatting Contexts:**
The browser determines the formatting context for each element (block, inline, flex, grid, table, etc.).
This context affects how child elements are laid out.

🌴 **Flow Calculation:**
The browser traverses the render tree top-down.
For each element, it calculates:
- Box model properties: width, height, margin, border, padding
- Position: top, left, right, bottom (for positioned elements)
- Stacking context: z-index, stacking order

🌴 **Dependency Resolution:**
Some elements depend on their children (e.g., a container with height: auto).
The browser may need to recursively calculate children before parents.

🌴 **Handling Special Layouts:**
- Floats: Elements floated with float are taken out of normal flow and affect siblings.
- Flexbox/Grid: The browser applies flex/grid algorithms to distribute space and align items.
- Tables: Table layout is handled with special rules for rows, columns, and cells.

🌴 **Viewport and Scrolling:**
The browser determines the size of the viewport and calculates scrollbars if content overflows.

🌴 **Fragmentation:**
For paged media (print, columns), the browser may split content into fragments.

🌳 **Output: Coordinates**
The result is a set of **exact coordinates and sizes for every box** in the render tree.

This information **is used in the next stage: painting**.


