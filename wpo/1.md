## Процесс рендера в браузерах
Когда пользователь загружает веб-страницу, браузер выполняет серию действий для отображения контента. Чтобы понять, как это происходит, давайте разберем процесс рендера пошагово.

🚀 Основные этапы процесса рендера
### 1. HTML → DOM (Document Object Model)
Браузер запрашивает и загружает HTML, затем парсит его и строит объектную модель документа (DOM).

Этапы преобразования:

- Чтение байтов из сетевого потока.
- Преобразование байтов в символы.
- Преобразование символов в токены (по сути, теги).
- Преобразование токенов в узлы DOM (JS-объекты).
- Формирование DOM-дерева, связывая узлы между собой в иерархию.

📌 В виде упрощённой схемы:
```
Bytes → Characters → Tokens → Nodes → DOM Tree
```

### 2. CSS → CSSOM (CSS Object Model)
При чтении HTML, браузер находит ссылки на CSS и выполняет схожий процесс:

- Чтение байтов.
- Преобразование байтов в символы.
- Преобразование символов в токены.
- Преобразование токенов в узлы CSSOM.
- Формирование CSSOM-дерева.
📌 Аналогичная схема:
```
Bytes → Characters → Tokens → Nodes → CSSOM Tree
```

### 3. Другие процессы
- Компиляция JavaScript: JS интерпретируется, компилируется, парсится и выполняется.
- Формирование Accessibility Tree: строится дерево доступности для функций адаптивности.
- Загрузка других ресурсов: шрифты, изображения, видео. Они могут быть как блокирующими (например, шрифты), так и неблокирующими.


### 4. Создание Render Tree
После построения DOM и CSSOM, они объединяются в **Render Tree**, который включает:

- Элементы DOM с применёнными стилями.
- Элементы, видимые на экране (не включает элементы display: none).
Это ключевой этап для отрисовки страницы.

### 5. Layout/Reflow
Браузер вычисляет размеры и позиции всех элементов на странице. Этот процесс включает:

- Размеры объектов.
- Положение объектов относительно друг друга.

### 6. Paint
На этом этапе браузер выполняет растеризацию (рисование пикселей) элементов Render Tree для отображения на экране.

### 7. Композиция
Последний этап включает сборку слоёв, например GPU-слоев, которые могут перекрывать друг друга.

```
DOM → CSSOM (параллельно) → JavaScript → Accessibility Tree → Render Tree → Layout → Paint → Composition
```

🛑 Критические ресурсы
Критические ресурсы — те, которые блокируют построение DOM. Обычно это:

- HTML
- CSS
- JavaScript
- Иногда шрифты.

  
#### Метрики критических ресурсов:
- Critical resources number — количество рендер-блокирующих ресурсов.
- Critical bytes number — общее количество блокирующих байтов.
- Critical path length — количество сетевых запросов (раунд-трипов).


### Способы улучшения рендера
1. Минимизация числа критических ресурсов
- Инлайнить ресурсы: встраивание CSS/JS прямо в HTML.
- Разделить и отложить загрузку неблокирующих ресурсов.

2. Минимизация критических байтов
- Сжатие (compress) — использование gzip/brotli.
- Минификация (minify) — удаление пробелов, комментариев.
- Исключение лишнего — загрузка только необходимых ресурсов.

3. Сокращение длины критического пути
- Минимизация запросов к внешним доменам.
- Использование HTTP/2.
- Единый сервер изображения/CSS/JavaScript.


🎨 Оптимизация специфичных ресурсов

#### HTML
- Минификация.
- Сжатие.

#### CSS
- Минификация.
- Разделение на медиа-запросы (screen, print).
- Сжатие.
- Инлайнить критические стили.
- Исключение неиспользуемого кода.

#### JavaScript
- Минификация.
- Сжатие.
- Разделение и установка async / defer.
async — выполнение после DOMContentLoaded, в любом порядке.
defer — выполнение после DOMContentLoaded, в порядке их определения.
- Инлайнить критические скрипты.

#### Шрифты
- Использовать `@font-face { display: fallback }`.
- Исключить лишние стили (например, дополнительные символы Unicode).
- Загрузка шрифтов с собственного домена.

🔗 Предзагрузка ресурсов
#### Виды предзагрузки и их особенности:
1. preload
Указывает браузеру скачать ресурс немедленно, без применения (CSS, JS, шрифты).
Обязательно для выполнения браузером.
2. prefetch
Загружает ресурс, который потребуется позже.
Опционально для браузера.
3. preconnect
Подготовка сетевого соединения (TCP/TLS).
4. dns-prefetch
Оптимизация DNS-запросов.
5. prerender
Загрузка и отображение указанной страницы.


🚀 Способы реализации предзагрузки
#### HTML:
```
<link rel="preload" as="style" href="/css/style.css">
```

#### HTTP-заголовок:
```
Link: </css/style.css>; rel="preload"; as="style"
```

#### Webpack:
```
import(/* webpackPreload: true */ './CriticalChunk');
```

### Приоритеты загрузки
Браузер оценивает приоритет ресурсов:

- highest (самый высокий)
- high
- medium
- low
- lowest
- idle

[Critical Rendering Path Explained YouTube!!!](https://www.youtube.com/watch?v=PkOBnYxqj3k)




